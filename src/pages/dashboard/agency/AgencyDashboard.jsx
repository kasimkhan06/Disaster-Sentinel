import React, { useState } from "react";
import {
  Card,
  Typography,
  Container,
  CssBaseline,
  Grid,
  TextField,
  Box,
  Button,
  IconButton,
  MenuItem,
  FormControl,
  InputLabel,
  Select,
} from "@mui/material";

const AgencyDashboard = () => {
  return(
  <Container component="main" maxWidth="md">
    <Typography variant="h4" sx={{ mt: 5, mb: 3, mt: 10, textAlign: "center" }}>
      Agency Dashboard Page
    </Typography>
  </Container>
  );
};

export default AgencyDashboard;